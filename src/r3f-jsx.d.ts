/// <reference types="@react-three/fiber" />

// Ensure R3F's global JSX.IntrinsicElements augmentation is visible to the TS compiler
// for the entire project (including files outside of the module graph).
export {};

